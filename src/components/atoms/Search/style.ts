import styled from "styled-components";

export const BasicSearch = styled.div`
  width: 300px;
  display: flex;
  justify-content: flex-start;
  align-items: center;
  gap: 1rem;
  padding: 0.7rem 0.8rem;
  background: transparent;
  svg {
    fill: #ffffff;
  }
`;
